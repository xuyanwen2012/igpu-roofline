# C/C++ runner style (runner/src). Shaders are not formatted: their SPIR-V hash keys results.
BasedOnStyle: LLVM
ColumnLimit: 100
# Include order is load-bearing (vulkan.h before the helpers that use it).
SortIncludes: Never
